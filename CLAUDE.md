# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Purpose

This repository contains a script to setup tmux and vim configurations consistently across multiple machines.

## Repository Structure

This is a configuration management repository focused on:
- tmux configuration setup
- vim configuration setup  
- Consistent environment setup across different machines

The main deliverable is a single setup script that can be executed on any machine to replicate the same tmux/vim development environment.